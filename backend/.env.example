# Clave secreta para firmar tokens JWT
JWT_SECRET=

# Tiempo de expiración del token (ej: 1h, 30m, 2d)
JWT_EXPIRATION=

# Credenciales del correo usado para enviar mails (ej: nodemailer)
MAIL_USER=
MAIL_PASS=

# URL del frontend que se usará como origen permitido o para redirecciones
FRONTEND_URL=
